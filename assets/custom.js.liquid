const heroImg = [...document.querySelectorAll('section.hero img')];
heroImg.forEach(function(img) {
  const html = `<link rel="preload" as="image" href="${img.src}">`;
  document.querySelector('head').insertAdjacentHTML('afterbegin', html);
})